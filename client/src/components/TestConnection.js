import React from 'react';

const TestConnection = () => {
    return (
        <div>
            <p style={{ color: 'blue' }}>
                Frontend is working! (Backend not connected)
            </p>
        </div>
    );
};

export default TestConnection; 